{
  "name": "podcast-cache",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy:checker": "cd src/checker && wrangler deploy",
    "deploy:frontend": "cd src/frontend && wrangler deploy",
    "dev:checker": "cd src/checker && wrangler dev",
    "dev:frontend": "cd src/frontend && wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.22.1"
  }
}
